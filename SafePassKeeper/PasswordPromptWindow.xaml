<Window x:Class="SafePassKeeper.PasswordPromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Security Verification" Width="400"
        SizeToContent="Height"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        FontFamily="Segoe UI, Tahoma, Geneva, Verdana, sans-serif"
        Background="#F3F3F3">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5,15,0,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.85"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#2D2D30" Padding="15,12">
            <TextBlock Text="Master Password" Foreground="White" FontSize="16" FontWeight="SemiBold" Margin="0"/>
        </Border>

        <StackPanel Grid.Row="1" Margin="25,20,25,20">
            <TextBlock Text="Please enter your password to decrypt the file:" Foreground="#333333" FontWeight="SemiBold" Margin="0,0,0,10" FontSize="13"/>

            <PasswordBox x:Name="PasswordBox" Padding="8" BorderBrush="#CCCCCC" BorderThickness="1" FontSize="14" Background="White" Margin="0,0,0,15"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel" Click="CancelButton_Click" Background="#9E9E9E" Width="80"/>
                <Button Content="Unlock" Click="OkButton_Click" Background="#0078D7" Width="80"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>