<Window x:Class="SafePassKeeper.AddAccountWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SafePassKeeper"
        mc:Ignorable="d"
        Title="Account Details" Height="620" Width="450"
        WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI, Tahoma, Geneva, Verdana, sans-serif"
        Background="#F3F3F3" Icon="/icon.ico">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#0078D7"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="20,8"/>
            <Setter Property="Margin" Value="5,15,5,5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1084E3"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,12,0,5"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>

        <Style TargetType="Control" x:Key="InputStyle">
            <Setter Property="Padding" Value="8"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        <Style TargetType="TextBox" BasedOn="{StaticResource InputStyle}"/>
        <Style TargetType="PasswordBox" BasedOn="{StaticResource InputStyle}"/>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#2D2D30" Padding="15,12">
            <TextBlock Text="Account Information" Foreground="White" FontSize="18" FontWeight="SemiBold" Margin="0"/>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="25,10,25,25">

                <TextBlock Text="Account Name:"/>
                <TextBox x:Name="AccountNameTextBox"/>

                <TextBlock Text="Username / Email:"/>
                <TextBox x:Name="UsernameTextBox"/>

                <TextBlock Text="Password:"/>
                <PasswordBox x:Name="PasswordBox"/>

                <TextBlock Text="Domain / URL:"/>
                <TextBox x:Name="DomainTextBox"/>

                <TextBlock Text="Note:"/>
                <TextBox x:Name="NoteTextBox" Height="100" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
                    <Button Content="Cancel" Click="CancelButton_Click" Background="#9E9E9E"/>
                    <Button Content="Save Account" Click="OkButton_Click" Background="#4CAF50"/>
                </StackPanel>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>